# IBKR ML Trader – Python 3.11 dependencies
# Install: pip install -r requirements.txt

# Web framework
flask>=3.0.0

# IBKR connectivity
ib_insync>=0.9.86     # connects to IB Gateway
nest_asyncio>=1.6.0   # required by ib_insync on Python 3.10+ (including 3.11)

# Data
pandas>=2.0.0
numpy>=1.24.0
yfinance>=0.2.28      # fallback data source when IB Gateway is not running

# Machine Learning
scikit-learn>=1.3.0
joblib>=1.3.0

# Utilities
python-dotenv>=1.0.0

# ── Removed ───────────────────────────────────────────────────────────────────
# pandas-ta was removed: it uses DataFrame.append() which was dropped in
# pandas 2.0, causing runtime errors on Python 3.11 + modern pandas.
# All technical indicators are now computed with pure pandas / numpy.
